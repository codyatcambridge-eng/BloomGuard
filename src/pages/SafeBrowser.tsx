import { NativeWebViewBrowser } from "@/components/browser/NativeWebViewBrowser";

/**
 * SafeBrowser - The unified GoodCreation Browser
 * Uses native WebView architecture for full interactivity
 * All safety layers are integrated within NativeWebViewBrowser
 */
const SafeBrowser = () => {
  return <NativeWebViewBrowser />;
};

export default SafeBrowser;
