import { NativeWebViewBrowser } from "@/components/browser/NativeWebViewBrowser";
/**
 * SafeBrowser route - use the unified native/web browser UI directly
 */
const SafeBrowser = () => <NativeWebViewBrowser />;

export default SafeBrowser;
